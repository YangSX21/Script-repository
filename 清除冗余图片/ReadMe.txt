脚本思路，如果在文件夹中只有一个png，则确定不能删除，如果有两个相同大小的png，能删掉其中的一个，有两个
不同大小的png，则无法进行进行确认是否删除，因为有2x，3x存在状况，有三个不同大小的png，删除最小的那个。

脚本使用，参数一是图片所在文件夹，是指.xcassets文件夹，参数二是输出的结果文档（就是指脚本认为可以删除的图片）

选用：可以把参数三和最后一行的注释去掉，参数三是备份文件文件夹地址，这样会直接删掉脚本认为可以删除的文件。（不太推荐这么做。）